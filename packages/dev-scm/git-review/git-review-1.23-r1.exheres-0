# Copyright 2013 Jorge Aparicio
# Distributed under the terms of the GNU General Public License v2

require pypi
# TODO we should be using has_bin=true has_lib=false here, but we haven't
# decided on the "correct" behavior of the python exlib under that conditions
require setup-py [ import=setuptools multibuild=false ]

SUMMARY="A git command for submitting branches to Gerrit"
DESCRIPTION="
git-review is a tool that helps submitting git branches to gerrit for review.
"
HOMEPAGE="https://launchpad.net/${PN}"

LICENCES="Apache-2"
SLOT="0"
PLATFORMS="~amd64"
MYOPTIONS=""

DEPENDENCIES="
    build+run:
        python_abis:2.6? ( dev-python/argparse[python_abis:2.6] )
        python_abis:3.1? ( dev-python/argparse[python_abis:3.1] )
"

src_test() {
    # no test suite provided by upstream
    # this is to avoid setuptools complaining about the lack of external
    # argparse for python 2.7 3.2 3.3, since setuptools can't handle per
    # version dependencies, and argparse is part of the standard library in
    # those versions
    :
}

